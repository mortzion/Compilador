first(cmd_composto) = BEGIN;

first(lsta_cmd) = PTO_VIRGULA, vazio

first(cmd) = BEGIN, IF, WHILE

first(cmd_cond) = IF

first(cmd_cond2) = ELSE, vazio

first(loop) = WHILE

======================================

next(cmd_composto) = next(cmd);

next(lsta_cmd) = END

next(cmd) = first(lsta_cmd), first(cmd_condicional2), next(loop), next(cond2);

next(cmd) = PTO_VIRGULA, END, ELSE, next(cmd);

next(cond) = next(cmd);

next(cond2) = next(cmd);

next(loop) = next(cmd);

